import React from "react";
import Headers from "../Headers";

const TableHead = () => {
  return (
    <React.Fragment>
      <thead>
        <Headers />
      </thead>
    </React.Fragment>
  );
};

export default TableHead;
